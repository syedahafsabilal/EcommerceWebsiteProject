import React from 'react';
import './styles.css'; // Import the CSS file

const CustomButton = () => {
  return (
    <div className="button">
      <span className="button-text">Show more car</span>
    </div>
  );
};

export default CustomButton;
